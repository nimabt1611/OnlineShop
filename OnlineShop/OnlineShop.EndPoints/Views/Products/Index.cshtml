@model List<OnlineShop.Domain.Products.Product>

@if (Model != null && Model.Count > 0)
{
	<table class="table table-bordered table-striped">
		<tr>
			<th>Your Product Name</th>
		
		</tr>

		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Name</td>
		

			</tr>
		}
	</table>

}